CREATE TABLE USER(ID INT NOT NULL AUTO_INCREMENT, NAME VARCHAR(20) NOT NULL, ENABLED INT DEFAULT 1, ROLE VARCHAR(10) NOT NULL, PASSWORD VARCHAR(70) NOT NULL, PRIMARY KEY(ID));

CREATE TABLE MOVIE(ID INT NOT NULL AUTO_INCREMENT, TITLE VARCHAR(20) NOT NULL, DIRECTOR VARCHAR(20), GENRE VARCHAR(20), VOTE INT DEFAULT 0, PRIMARY KEY(ID));
